<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/card_margin"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="@dimen/card_elevation"
    app:cardBackgroundColor="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Cocktail Image Container -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/cocktail_image_height">

            <ImageView
                android:id="@+id/iv_cocktail"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/placeholder"
                tools:src="@drawable/cocktail_logo" />

            <!-- Favorite Icon -->
            <ImageView
                android:id="@+id/iv_favorite"
                android:layout_width="@dimen/dp_28"
                android:layout_height="@dimen/dp_28"
                android:layout_gravity="top|end"
                android:layout_margin="@dimen/icon_margin"
                android:background="@drawable/favorite_ripple"
                android:padding="@dimen/icon_padding"
                android:src="@drawable/ic_favorite_border_black_24dp"
                android:contentDescription="@string/favorite_icon"
                android:clickable="true"
                android:focusable="true" />

        </FrameLayout>

        <!-- Cocktail Info -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/content_padding">

            <TextView
                android:id="@+id/tv_cocktail_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/black"
                android:textSize="@dimen/text_size_medium"
                android:textStyle="bold"
                tools:text="Cocktail Name" />

            <TextView
                android:id="@+id/tv_cocktail_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/text_margin_top_small"
                android:textColor="@color/black"
                android:textSize="@dimen/text_size_small"
                tools:text="Cocktail description" />

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView> 
